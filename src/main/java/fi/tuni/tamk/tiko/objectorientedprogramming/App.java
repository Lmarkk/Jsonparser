package fi.tuni.tamk.tiko.objectorientedprogramming;
import java.util.*;
/**
 * Class used for testing JSON parser functionality.
 * 
 * @author Lassi Markkinen
 * @version 2019.1216
 */
public class App {
    public static void main(String[] args) {
       
        /*
        JSONObject jsonObject = new JSONObject();
        jsonObject.put("firstName", "John");
        jsonObject.put("lastName", "Smith");

        // jsonObject = JsonParser.parse("{\"firstName\":\"John\",\"lastName\":\"Smith\"}");
        // JsonParser.parse(jsonObject);

        
        jsonObject.put("hasChildren", true);
    
        JSONObject homeNumber = new JSONObject("type", "home", "number", "212 555-1234");
        JSONObject officeNumber = new JSONObject("type", "office", "number", "646 555-4567");

        JSONObject testmultiple = new JSONObject();
        testmultiple.put("type", "personal"); 
        testmultiple.put("number", "999 555-8888");

        List<JSONObject> numbers = List.of(homeNumber, officeNumber);
        jsonObject.put("phoneNumbers", numbers);
        jsonObject.put("secretNumber", testmultiple);

        List<String> workingPlaces = List.of("x", "y", "z");
        jsonObject.put("workingPlaces", workingPlaces);

        System.out.println(JsonParser.parse(jsonObject));
        
        
        String testJson = "[{\"juu\":1}]";
        
        System.out.println(JsonParser.parseShoppingList(testJson));

        */
        
    }
}
